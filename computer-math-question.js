const question={text:'একটি সংখ্যার 25% = 20 হলে সংখ্যাটি কত?',options:['40','60','80','100'],answer:'80'};q.innerHTML=`<h2>${question.text}</h2><div class="answers">${question.options.map((x,i)=>`<label style="display:block;margin:8px 0;padding:12px;border:1px solid var(--line);border-radius:12px"><input type="radio" name="ans" value="${x}"> ${String.fromCharCode(65+i)}) ${x}</label>`).join('')}</div>`;check.onclick=()=>{const v=document.querySelector('input[name=ans]:checked')?.value;if(!v){feedback.textContent='একটি উত্তর নির্বাচন করো।';return}feedback.textContent=v===question.answer?'✓ সঠিক উত্তর':'✕ ভুল উত্তর — সঠিক উত্তর: '+question.answer;feedback.className=v===question.answer?'success':'danger';};
